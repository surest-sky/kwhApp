import 'package:flutter/material.dart';
import 'dart:ui';

class Styles {
  static Color styleBackgroundGray = Colors.grey.shade100;
  static double styleMediaPTop = MediaQueryData.fromWindow(window).padding.top;
}